{
  "name": "module-federation-modern",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nx/js": "22.5.1",
    "@swc-node/register": "1.11.1",
    "@swc/core": "1.15.8",
    "@swc/helpers": "0.5.18",
    "nx": "22.5.1",
    "prettier": "^2.6.2"
  },
  "scripts": {
    "app-verbose": "pnpm nx run-many --target=serve --configuration=development -p host,remote-app,auth-app,showcase-ssg,bff-web --parallel=5 --output-style=stream --verbose",
    "build": "pnpm nx run-many --target=build --configuration=development -p host,remote-app,auth-app,showcase-ssg,bff-web --parallel=5 --output-style=stream --verbose"
  },
  "workspaces": [
    "packages/*"
  ]
}
